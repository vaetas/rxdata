name: example
description: rxdata example project
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  collection: ^1.15.0
  flutter:
    sdk: flutter
  hive_flutter: ^1.1.0
  http: ^0.13.4
  intl: ^0.17.0
  rxdata:
    path: ../

dev_dependencies:
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
